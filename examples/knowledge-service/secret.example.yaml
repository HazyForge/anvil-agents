apiVersion: v1
kind: Secret
metadata:
  name: knowledge-reader
  namespace: agents
type: Opaque
stringData:
  # Optional for APIs that are cluster-local and currently unauthenticated.
  # Do not mistake an omitted token for a read-only authorization boundary.
  KNOWLEDGE_BASE_TOKEN: replace-with-a-read-only-token
