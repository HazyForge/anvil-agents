apiVersion: control.anvil.hazyforge.io/v1alpha1
kind: AgentSkillSet
metadata:
  name: organization-knowledge
  namespace: agents
spec:
  description: Provider-neutral read-only organizational knowledge search.
  skills:
    - name: organization-knowledge
      description: Use current organizational context without inventing it.
      content: |
        Before proposing a change, run `.anvil-tools/knowledge-search` with a
        narrow query. Cite returned note IDs or paths in the report. Treat the
        service as read-only. If it is unavailable or has no relevant result,
        say so and continue from repository evidence; never invent guidance.
---
apiVersion: control.anvil.hazyforge.io/v1alpha1
kind: AgentToolSet
metadata:
  name: organization-knowledge-http
  namespace: agents
spec:
  description: Read-only search wrapper for an external operation-envelope API.
  tools:
    - name: knowledge-search
      description: Search the separately deployed knowledge service.
      setupScript: |
        command -v curl >/dev/null
        command -v jq >/dev/null
        mkdir -p .anvil-tools
        cat > .anvil-tools/knowledge-search <<'SCRIPT'
        #!/usr/bin/env sh
        set -eu
        if [ "${1:-}" = "--help" ]; then
          echo "usage: knowledge-search QUERY"
          exit 0
        fi
        test -n "${KNOWLEDGE_BASE_URL:-}"
        command -v curl >/dev/null
        command -v jq >/dev/null
        payload="$(jq -nc --arg query "$*" \
          '{operation:"search-index",params:{query:$query,limit:10}}')"
        set -- --fail --silent --show-error \
          --request POST \
          --header "Content-Type: application/json" \
          --header "Accept: application/json"
        if [ -n "${KNOWLEDGE_BASE_TOKEN:-}" ]; then
          set -- "$@" --header "Authorization: Bearer ${KNOWLEDGE_BASE_TOKEN}"
        fi
        curl "$@" --data "$payload" \
          "${KNOWLEDGE_BASE_URL%/}/v1/operations"
        SCRIPT
        chmod 0700 .anvil-tools/knowledge-search
      verifyCommand: [".anvil-tools/knowledge-search", "--help"]
---
apiVersion: control.anvil.hazyforge.io/v1alpha1
kind: AgentHarnessProfile
metadata:
  name: knowledge-codex
  namespace: agents
spec:
  description: Codex runtime with read-only knowledge-service identity.
  backend:
    kind: codex
  execution:
    serviceAccountName: agent-runner
    workdir: /workspace
    envSecretRefs:
      - name: codex-credentials
      - name: knowledge-reader
    extraEnv:
      - name: KNOWLEDGE_BASE_URL
        value: https://knowledge-api.example.com
    timeoutSeconds: 1800
---
apiVersion: control.anvil.hazyforge.io/v1alpha1
kind: AgentHarnessProfile
metadata:
  name: knowledge-pi
  namespace: agents
spec:
  description: Pi runtime with the same read-only knowledge-service identity.
  backend:
    kind: piAgent
    modelProvider: xai
    providerAuthMode: oauth
  execution:
    serviceAccountName: agent-runner
    workdir: /workspace
    envSecretRefs:
      - name: pi-credentials
      - name: knowledge-reader
    extraEnv:
      - name: KNOWLEDGE_BASE_URL
        value: https://knowledge-api.example.com
    timeoutSeconds: 1800
---
apiVersion: control.anvil.hazyforge.io/v1alpha1
kind: AgentRunProfile
metadata:
  name: knowledge-aware-reviewer
  namespace: agents
spec:
  description: Read-only repository review with external organizational context.
  scope:
    summary: Review a repository using organization runbooks as context.
  harnessProfileRef:
    name: knowledge-codex
  skillSets:
    refs:
      - name: organization-knowledge
  toolSets:
    refs:
      - name: organization-knowledge-http
  harness:
    intent: observe
    systemPrompt: Do not send secrets or unrelated source content to the knowledge service.
