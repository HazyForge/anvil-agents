helmChartPath: charts/anvil-agents

# Bootstrap is intentionally inert. The coordinated cutover pins the published
# digest, switches CRD ownership, scales this to one, and deploys the old
# operator build with embedded agent reconcilers removed in the same window.
replicaCount: 0
fullnameOverride: anvil-agents
image:
  reference: ghcr.io/hazyforge/anvil-agents:latest
  pullPolicy: IfNotPresent
imagePullSecrets:
  - name: ghcr-creds
runnerImages:
  codex: ghcr.io/hazyforge/anvil-agent-run-codex:latest
  hermesAgent: ghcr.io/hazyforge/anvil-agent-run-hermes:latest
  openClaw: ghcr.io/hazyforge/anvil-agent-run-openclaw:latest
  grokBuild: ghcr.io/hazyforge/anvil-agent-run-grok-build:latest
  piAgent: ghcr.io/hazyforge/anvil-agent-run-pi:latest
crds:
  install: false
leaderElection: true
applicationMaxConcurrentRuns: 4
defaultStorageClass: observability-local
externalSecrets:
  enabled: true
adverseSourceGVKs:
  - control.anvil.hazyforge.io/v1alpha1/ApplicationRelease
  - control.anvil.hazyforge.io/v1alpha1/ArtifactBuild
  - control.anvil.hazyforge.io/v1alpha1/IOSBuild
  - control.anvil.hazyforge.io/v1alpha1/AnvilTask
  - control.anvil.hazyforge.io/v1alpha1/TestRun
extraRBACRules:
  - apiGroups: [control.anvil.hazyforge.io]
    resources: [applicationreleases, artifactbuilds, iosbuilds, anviltasks, testruns]
    verbs: [get, list, watch]
archive:
  databaseURLSecret:
    name: anvilhub-inventory-database
    key: database-url
  terminalRetention: 72h
# The issuer and ZITADEL claim shapes are staged, but audience and authorization
# bindings intentionally remain empty. Enabling the API must fail until a
# reviewed change supplies a dedicated anvil-agents audience and namespace
# grants; do not reuse the interactive client ID or a wildcard compatibility
# binding in the production enablement.
api:
  enabled: false
  config:
    oidc:
      issuer: https://hazyforge1-azsbgb.us1.zitadel.cloud
    authorization:
      roleClaims:
        - roles
      roleObjectClaims:
        - urn:zitadel:iam:org:project:roles
        - urn:zitadel:iam:org:projects:roles
        - urn:zitadel:iam:org:project:376124473528572432:roles
extraVolumes:
  - name: anvilhub-inventory-database
    secret:
      secretName: anvilhub-inventory-database
extraVolumeMounts:
  - name: anvilhub-inventory-database
    mountPath: /etc/anvilhub/inventory-database
    readOnly: true
nodeSelector:
  kubernetes.io/arch: amd64
