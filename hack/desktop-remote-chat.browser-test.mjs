#!/usr/bin/env node
// UI contract test: isolated browser + in-memory HTTP fixture. No real OIDC,
// remote runner, or model is used. Build web/desktop first; install Playwright
// separately and set PLAYWRIGHT_MODULE to its absolute index.mjs when needed.
import assert from 'node:assert/strict';
import { createServer } from 'node:http';
import { readFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import { resolve, extname } from 'node:path';
const { chromium } = await import(process.env.PLAYWRIGHT_MODULE || 'playwright');
const dist = fileURLToPath(new URL('../web/desktop/dist/', import.meta.url));
const server = createServer(async (req, res) => {
  const path = new URL(req.url, 'http://fixture').pathname;
  const asset = path.startsWith('/assets/') ? resolve(dist, `.${path}`) : resolve(dist, 'index.html');
  try {
    res.setHeader('Content-Type', ({'.js':'application/javascript','.css':'text/css','.html':'text/html'})[extname(asset)] || 'application/octet-stream');
    res.end(await readFile(asset));
  } catch { res.writeHead(404).end(); }
});
await new Promise(resolve => server.listen(0, '127.0.0.1', resolve));
const origin = `http://127.0.0.1:${server.address().port}`;
const browser = await chromium.launch({headless:true, ...(process.env.PLAYWRIGHT_CHROMIUM_EXECUTABLE ? {executablePath:process.env.PLAYWRIGHT_CHROMIUM_EXECUTABLE} : {})});
const context = await browser.newContext();
const page = await context.newPage();
await page.clock.install();
page.setDefaultTimeout(10000);
const errors = [], posts = [], creates = [], threads = new Map();
let failNext = false, loseNextReceipt = false;
let createGate = null, appendGate = null, receiptGate = null;
const standing = new Map(), standingGates = new Map(), standingFailures = new Set();
const ensures = [];
const withheldDetails = new Map(), deniedDetails = new Map(), detailGets = new Map();
const eventPlans = new Map(), eventGets = new Map(), eventAuth = new Map(), heldDetailReads = new Map();
page.on('pageerror', error => errors.push(error.message));
await context.addInitScript(() => {
  localStorage.setItem('anvil-agents-desktop.chat-location', 'remote');
  sessionStorage.setItem('anvil-agents-desktop.accessToken', 'ui-contract-fixture-not-a-token');
  sessionStorage.setItem('anvil-agents-desktop.expiresAt', String(Date.now()+3600000));
});
const projectRosters = new Map();
const profile = name => ({apiVersion:'control.anvil.hazyforge.io/v1alpha1',kind:'AgentRunProfile',metadata:{name},spec:{harnessProfileRef:{name:'codex-standard'}}});
const harness = (name, kind) => ({kind:'AgentHarnessProfile',metadata:{name},spec:{backend:{kind}}});
const config = {defaultNamespaces:['anvilhub','hazy-trade'],oidc:{issuer:'https://fixture.invalid',clientId:'fixture-console',audiences:['fixture'],scopes:['openid']},desktop:{oidcClientId:'fixture-native'},composition:{readEnabled:true,writeEnabled:true},runs:{createEnabled:true},chat:{enabled:true}};
const reply = async (route, data, status=200) => route.fulfill({status,contentType:'application/json',body:JSON.stringify(data)});
await context.route('**/*', async route => {
  const request=route.request(), url=new URL(request.url()), path=url.pathname;
  if(url.origin!==origin) return route.abort();
  if(path==='/local/v1/snapshot') return reply(route,{productTitle:'Anvil Agents Desktop',prefs:{apiOrigin:origin},api:{origin,reachable:true},harnesses:[],wsl:{},harnessTarget:'native'});
  if(path==='/ui-config.json') return reply(route,config);
  if(!path.startsWith('/api/')) return route.continue();
  assert.ok(['Bearer ui-contract-fixture-not-a-token','Bearer ui-contract-refreshed-not-a-token','Bearer ui-contract-stream-refreshed-not-a-token'].includes(request.headers().authorization));
  assert.equal(url.searchParams.has('access_token'),false);
  const m=path.match(/^\/api\/v1\/namespaces\/([^/]+)\/(.*)$/);
  if(!m) return reply(route,{},404);
  const [,ns,tail]=m;
  if(tail==='agent-run-profiles') return reply(route,{items:projectRosters.get(ns) || [profile(ns==='anvilhub'?'agent-alpha':'agent-trade'),profile('agent-beta')]});
  if(tail==='agent-harness-profiles') return reply(route,{items:[harness('codex-standard','codex'),harness('agy-review','agy')]});
  if(tail==='chat/threads' && request.method()==='GET') return reply(route,{items:[...threads.values()].filter(t=>t.namespace===ns && (!url.searchParams.get('profileName') || t.profileName===url.searchParams.get('profileName')))});
  if(tail==='chat/threads' && request.method()==='POST') {
    const body=request.postDataJSON();
    if(body.standing) {
      const key=`${ns}/${body.profileName}`; ensures.push(key);
      if(standingGates.has(key)) await standingGates.get(key);
      if(standingFailures.has(key)) return reply(route,{error:{code:'temporarily_unavailable',message:'Fixture agent could not be opened'}},503);
      let id=standing.get(key), created=false;
      if(!id) {
        const eligible=[...threads.values()].reverse().find(t=>t.namespace===ns && t.profileName===body.profileName && !t.harnessProfileName && !t.metadata?.sourceThreadId);
        id=eligible?.id || `standing-${ns}-${body.profileName}`;
        if(!eligible) { threads.set(id,{id,namespace:ns,profileName:body.profileName,mode:'persona',title:body.profileName,createdBy:'fixture',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString(),messages:[],turns:[]}); created=true; }
        standing.set(key,id);
      }
      return reply(route,threads.get(id),created?201:200);
    }
    if(createGate) await createGate;
    creates.push({ns,...body});
    const t={id:`thread-${creates.length}`,namespace:ns,...body,createdBy:'fixture',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString(),messages:[],turns:[]};
    threads.set(t.id,t); return reply(route,t,201);
  }
  const tm=tail.match(/^chat\/threads\/([^/]+)(\/messages)?$/);
  if(tm) {
    const t=threads.get(tm[1]);
    if (!t || t.namespace!==ns) {
      errors.push(`Cross-namespace thread request: ${ns}/${tm[1]}`);
      return reply(route,{error:{code:'not_found',message:'Fixture thread is not in this namespace'}},404);
    }
    if(request.method()==='GET') {
      detailGets.set(t.id,(detailGets.get(t.id)||0)+1);
      if(deniedDetails.has(t.id)) return reply(route,{error:{code:'not_found',message:'Fixture access denied'}},deniedDetails.get(t.id));
      const snapshot=structuredClone(withheldDetails.get(t.id)||t);
      // Match the live API's empty persisted Go slices for new standing chats.
      if(snapshot.messages.length===0) snapshot.messages=null;
      if(snapshot.turns.length===0) snapshot.turns=null;
      const held=heldDetailReads.get(t.id);
      if(held) {heldDetailReads.delete(t.id); held.started(); await held.gate;}
      return reply(route,snapshot);
    }
    if(appendGate) await appendGate;
    const body=request.postDataJSON(); posts.push({thread:t.id,...body});
    if(failNext) {failNext=false;return reply(route,{error:{code:'temporarily_unavailable',message:'Fixture temporary transport failure'}},503);}
    if(loseNextReceipt) withheldDetails.set(t.id,structuredClone(t));
    let turn=t.turns.find(x=>x.id===body.requestId);
    let user=t.messages.find(x=>x.id===`user-${body.requestId}`);
    if(!turn) {
      user={id:`user-${body.requestId}`,threadId:t.id,role:'user',content:body.content,sequence:t.messages.length+1,createdAt:new Date().toISOString()};
      turn={id:body.requestId,requestId:body.requestId,userMessageId:user.id,status:'queued',runName:`fixture-run-${t.id}-${t.turns.length+1}`}; t.messages.push(user); t.turns.push(turn); t.activeTurn=turn;
    }
    if(loseNextReceipt) {loseNextReceipt=false;return reply(route,{error:{code:'receipt_lost',message:'Fixture accepted receipt was lost'}},503);}
    withheldDetails.delete(t.id);
    // Capture the actual accept-time response; later completion cannot mutate it.
    const receipt=structuredClone({thread:t,user,turn});
    if(receiptGate) await receiptGate;
    return reply(route,receipt,202);
  }
  const em=tail.match(/^agent-runs\/([^/]+)\/events$/);
  if(em) {
    const count=(eventGets.get(em[1])||0)+1; eventGets.set(em[1],count);
    eventAuth.set(em[1],[...(eventAuth.get(em[1])||[]),request.headers().authorization]);
    const plans=eventPlans.get(em[1]); const plan=plans?.[Math.min(count-1,plans.length-1)];
    if(plan?.refreshSession) await page.evaluate(()=>sessionStorage.setItem('anvil-agents-desktop.accessToken','ui-contract-stream-refreshed-not-a-token'));
    if(plan?.status===404) return reply(route,{error:{message:'Runner has not appeared yet'}},404);
    return route.fulfill({status:200,contentType:'text/event-stream',body:plan?.body||'event: complete\ndata: {}\n\n'});
  }
  return reply(route,{},404);
});
const visible = async text => page.getByText(text,{exact:true}).waitFor({state:'visible'});
const draftVisible = async content => page.waitForFunction(value => { const input=document.querySelector('textarea[aria-label="Message"]'); return input && !input.disabled && input.value===value; }, content);
const openDetails = async name => {
  const summary=page.locator('summary').filter({hasText:new RegExp(`^${name}$`)});
  if(!await summary.evaluate(el=>el.parentElement.open)) await summary.click();
};
const configuration = () => openDetails('Conversation details');
const harnessChat = async () => {await openDetails('Other chats'); await page.getByRole('button',{name:'Chat with a harness',exact:true}).click(); await configuration();};
const newConversation = async () => {await configuration(); await page.getByRole('button',{name:'Start a separate conversation',exact:true}).click(); await configuration();};
const openAgent = async name => {
  await page.locator(`.agent-row[title="${name}"]`).click();
  await page.waitForFunction(name=>document.querySelector('select[aria-label="Agent"]')?.value===name && !document.querySelector('textarea[aria-label="Message"]')?.disabled,name);
};
const openHistory = async id => {
  await openAgent(threads.get(id).profileName);
  await configuration();
  await page.getByLabel('Conversation history',{exact:true}).selectOption(id);
  await page.waitForFunction(()=>!document.querySelector('textarea[aria-label="Message"]')?.disabled);
};
const namespace = async name => {await page.getByLabel('Project',{exact:true}).selectOption(name); await page.waitForFunction(()=>!document.querySelector('textarea[aria-label="Message"]')?.disabled);};
const complete = (id,text,failed=false) => {
  const t=threads.get(id); const turn=t.activeTurn;
  turn.status=failed?'failed':'succeeded'; if(failed)turn.error=text;
  else t.messages.push({id:`assistant-${turn.id}`,threadId:id,role:'assistant',content:text,sequence:t.messages.length+1,createdAt:new Date().toISOString()});
  delete t.activeTurn;
};
try {
  await page.goto(`${origin}/chat`);
  await page.getByLabel('Agent',{exact:true}).locator('option').filter({hasText:'agent-alpha'}).waitFor({state:'attached'});
  await draftVisible('');
  assert.equal(await page.locator('.agent-row').count(),2);
  assert.equal(standing.get('anvilhub/agent-alpha'),'standing-anvilhub-agent-alpha');
  await newConversation();
  await page.getByLabel('Agent',{exact:true}).selectOption('agent-alpha');
  await page.getByLabel('Remote harness').selectOption('agy-review');
  await page.getByLabel('Role',{exact:true}).selectOption('fleet');
  await page.getByLabel('Allow this agent to delegate messages').check();
  await page.getByLabel('Message',{exact:true}).fill('Coordinate the fixture agents');
  assert.equal(await page.getByRole('button',{name:'Send',exact:true}).isDisabled(),true);
  await page.getByLabel('agent-beta',{exact:true}).check();
  await page.getByLabel('Message',{exact:true}).fill('Coordinate the fixture agents');
  let releaseCreate, releaseAppend;
  createGate=new Promise(resolve=>{releaseCreate=resolve;});
  appendGate=new Promise(resolve=>{releaseAppend=resolve;});
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('article',{name:'Your pending message'}).getByText('Coordinate the fixture agents',{exact:true}).waitFor();
  await page.getByRole('status').filter({hasText:'Saving conversation…'}).waitFor();
  assert.equal(creates.length,0);
  releaseCreate(); createGate=null;
  await page.getByRole('status').filter({hasText:'Sending message…'}).waitFor();
  assert.equal(posts.length,0);
  assert.equal(await page.getByRole('article',{name:'Your pending message'}).count(),1);
  releaseAppend(); appendGate=null;
  await page.getByRole('status').filter({hasText:'Message received; waiting for the agent to start'}).waitFor();
  assert.deepEqual(creates[0],{ns:'anvilhub',profileName:'agent-alpha',harnessProfileName:'agy-review',mode:'fleet',title:'Manager · agent-alpha',metadata:{coordination:{enabled:true,allowedProfiles:['agent-beta']}}});
  assert.equal(await page.getByRole('button',{name:'Waiting for reply…'}).isDisabled(),true);
  assert.equal(await page.getByText('Coordinate the fixture agents',{exact:true}).count(),1);
  console.log('PASS slow create/append show immediate user bubble and honest saving/sending status');
  complete('thread-1','Fixture coordination response');
  await visible('Fixture coordination response');
  await page.reload();
  await visible('Fixture coordination response');
  assert.equal(await page.getByLabel('Remote harness').inputValue(),'agy-review');
  assert.equal(await page.getByLabel('Role',{exact:true}).inputValue(),'fleet');
  assert.equal(await page.getByLabel('Allow this agent to delegate messages').isChecked(),true);
  assert.equal(await page.getByLabel('agent-beta',{exact:true}).isChecked(),true);
  console.log('PASS manager/agent/harness selection; asynchronous reply; persisted history reload');
  await page.evaluate(()=>sessionStorage.setItem('anvil-agents-desktop.accessToken','ui-contract-refreshed-not-a-token'));
  const refreshedRequest=page.waitForRequest(r=>r.url().includes('/chat/threads/thread-1') && r.headers().authorization==='Bearer ui-contract-refreshed-not-a-token');
  await page.clock.fastForward(60001);
  await refreshedRequest;
  await visible('Fixture coordination response');
  assert.equal(await page.getByLabel('Agent',{exact:true}).isDisabled(),true);
  assert.equal(await page.getByLabel('Agent',{exact:true}).inputValue(),'agent-alpha');
  console.log('PASS token refresh preserves selected conversation and history');

  await newConversation();
  await page.getByLabel('Allow this agent to delegate messages').uncheck();
  await page.getByLabel('Agent',{exact:true}).selectOption('agent-beta');
  await page.getByLabel('Remote harness').selectOption('');
  await page.getByLabel('Role',{exact:true}).selectOption('persona');
  await page.getByLabel('Message',{exact:true}).fill('Retry this fixture message');
  failNext=true;
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('alert').filter({hasText:'Fixture temporary transport failure'}).waitFor();
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Retry this fixture message');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('status').filter({hasText:'Message received; waiting for the agent to start'}).waitFor();
  assert.equal(posts.at(-1).requestId,posts.at(-2).requestId);
  assert.equal(creates.length,2); assert.equal(threads.get('thread-2').messages.length,1);
  complete('thread-2','Fixture runner failed',true);
  await visible('Turn failed: Fixture runner failed');
  await page.getByLabel('Message',{exact:true}).fill('Continue after runner failure');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('status').filter({hasText:'Message received; waiting for the agent to start'}).waitFor();
  assert.notEqual(posts.at(-1).requestId,posts.at(-2).requestId);
  complete('thread-2','Fixture second conversation response');
  await visible('Fixture second conversation response');
  const earlierFailure = page.getByText('Turn failed: Fixture runner failed',{exact:true});
  assert.equal(await earlierFailure.isVisible(),false);
  await page.getByText('Earlier failed turns (1)',{exact:true}).click();
  await earlierFailure.waitFor({state:'visible'});
  await page.getByText('Earlier failed turns (1)',{exact:true}).click();
  console.log('PASS transient failure retry reuses idempotency key; runner failure accepts a new turn and earlier errors collapse');
  const priorMessages=threads.get('thread-2').messages.length;
  await page.getByLabel('Message',{exact:true}).fill('Ambiguous accepted fixture message');
  loseNextReceipt=true;
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('alert').filter({hasText:'Fixture accepted receipt was lost'}).waitFor();
  const ambiguousID=posts.at(-1).requestId;
  assert.equal(threads.get('thread-2').messages.length,priorMessages+1);
  await openHistory('thread-1');
  await visible('Fixture coordination response');
  await openAgent('agent-beta');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Ambiguous accepted fixture message');
  await page.reload();
  await visible('Fixture second conversation response');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Ambiguous accepted fixture message');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('status').filter({hasText:'Message received; waiting for the agent to start'}).waitFor();
  assert.equal(posts.at(-1).requestId,ambiguousID);
  assert.equal(threads.get('thread-2').messages.length,priorMessages+1);
  assert.equal(creates.length,2);
  complete('thread-2','Fixture ambiguous receipt recovered');
  await visible('Fixture ambiguous receipt recovered');
  console.log('PASS lost accepted receipt retains draft and idempotency through sidebar and reload');
  await page.getByLabel('Message',{exact:true}).fill('  Accepted receipt reconciled without retry  ');
  loseNextReceipt=true;
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('alert').filter({hasText:'Fixture accepted receipt was lost'}).waitFor();
  const countBeforeReceipt=posts.length;
  withheldDetails.delete('thread-2');
  await page.getByRole('article',{name:'Your pending message'}).waitFor({state:'hidden'});
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'');
  assert.equal(await page.getByRole('alert').count(),0);
  assert.equal(posts.length,countBeforeReceipt);
  assert.equal(await page.getByText('Accepted receipt reconciled without retry',{exact:true}).count(),1);
  complete('thread-2','Fixture receipt confirmed');
  await visible('Fixture receipt confirmed');
  let releaseReceipt;
  receiptGate=new Promise(resolve=>{releaseReceipt=resolve;});
  await page.getByLabel('Message',{exact:true}).fill('Slow accepted HTTP response');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('status').filter({hasText:'Message received; waiting for the agent to start'}).waitFor();
  complete('thread-2','Fast reply before accepted HTTP response');
  await visible('Fast reply before accepted HTTP response');
  releaseReceipt(); receiptGate=null;
  await page.getByRole('button',{name:'Send',exact:true}).waitFor();
  assert.deepEqual((await page.locator('.chat-bubble-body').allTextContents()).slice(-2),['Slow accepted HTTP response','Fast reply before accepted HTTP response']);
  await page.getByLabel('Message',{exact:true}).fill('Next message remains sendable');
  assert.equal(await page.getByRole('button',{name:'Send',exact:true}).isEnabled(),true);
  console.log('PASS observed receipt clears uncertain state; stale queued202 preserves completed reply and enables next send');

  let releaseStaleRead, staleReadStarted;
  const staleReadStart=new Promise(resolve=>{staleReadStarted=resolve;});
  const staleReadGate=new Promise(resolve=>{releaseStaleRead=resolve;});
  heldDetailReads.set('thread-2',{started:staleReadStarted,gate:staleReadGate});
  await page.clock.fastForward(3001);
  await staleReadStart;
  await page.getByLabel('Message',{exact:true}).fill('Accepted while an older read is in flight');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('button',{name:'Waiting for reply…'}).waitFor();
  const staleResponse=page.waitForResponse(r=>r.url().endsWith('/chat/threads/thread-2') && r.request().method()==='GET');
  releaseStaleRead();
  await staleResponse;
  // Let React commit this response before checking the accepted view.
  await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));
  assert.equal(await page.getByText('Accepted while an older read is in flight',{exact:true}).count(),1);
  assert.equal(await page.getByRole('button',{name:'Waiting for reply…'}).isDisabled(),true);
  await page.getByLabel('Message',{exact:true}).fill('Must wait for the active reply');
  assert.equal(await page.getByRole('button',{name:'Waiting for reply…'}).isDisabled(),true);
  complete('thread-2','Fixture reply after stale read');
  await visible('Fixture reply after stale read');
  console.log('PASS pre-send GET arriving after accepted POST preserves user bubble and active turn');
  const activityRun=`fixture-run-thread-2-${threads.get('thread-2').turns.length+1}`;
  const event=(name,body)=>`event: ${name}\ndata: ${JSON.stringify(body)}\n\n`;
  const terminalTail=event('snapshot',{run:{phase:'Succeeded'}})+
    event('log',{timestamp:new Date().toISOString(),line:JSON.stringify({type:'tool_use',part:{id:'safe-read',tool:'read',state:{input:{path:'RAW_PRIVATE_PATH_SENTINEL'}}}})})+
    event('log',{line:'UNKNOWN_PRIVATE_RAW_SENTINEL'})+event('complete',{});
  eventPlans.set(activityRun,[{status:404},{body:terminalTail}]);
  await page.getByLabel('Message',{exact:true}).fill('Observe the reported runner activity');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await visible('Waiting for the remote runner to become available…');
  await page.clock.fastForward(3001);
  const activityRegion=page.getByRole('region',{name:'Agent activity'});
  await activityRegion.getByText('Reading files',{exact:true}).waitFor();
  assert.equal(await activityRegion.getByRole('status').innerText(),'Harness finished; saving the reply');
  assert.equal(await page.getByText(/RAW_PRIVATE_PATH_SENTINEL|UNKNOWN_PRIVATE_RAW_SENTINEL/).count(),0);
  assert.equal(await page.getByText('Waiting for the remote runner to become available…',{exact:true}).count(),0);
  assert.equal(eventGets.get(activityRun),2);
  await page.clock.fastForward(6001);
  assert.equal(eventGets.get(activityRun),2);
  const rawRequest=page.waitForResponse(r=>r.url().includes(`/agent-runs/${activityRun}/events`));
  await page.getByText('Runner activity',{exact:true}).click();
  await rawRequest;
  assert.equal(eventGets.get(activityRun),3);
  await page.getByText('Runner activity',{exact:true}).click();
  complete('thread-2','Fixture streamed reply');
  await visible('Fixture streamed reply');
  console.log('PASS real events endpoint retries initial404, shows safe tool labels, keeps terminal headline, and opens raw SSE only on demand');

  const authRun=`fixture-run-thread-2-${threads.get('thread-2').turns.length+1}`;
  eventPlans.set(authRun,[{refreshSession:true,body:event('error',{code:'token_expired'})},{body:event('snapshot',{run:{phase:'Succeeded'}})+event('complete',{})}]);
  await page.getByLabel('Message',{exact:true}).fill('Reconnect activity with the refreshed session');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await visible('Refreshing your session to reconnect activity…');
  await page.clock.fastForward(3001);
  await activityRegion.getByRole('status').filter({hasText:'Harness finished; saving the reply'}).waitFor();
  assert.deepEqual(eventAuth.get(authRun),[eventAuth.get(activityRun)[0],'Bearer ui-contract-stream-refreshed-not-a-token']);
  assert.notEqual(eventAuth.get(authRun)[0],eventAuth.get(authRun)[1]);
  complete('thread-2','Fixture refreshed activity reply');
  await visible('Fixture refreshed activity reply');
  await page.evaluate(()=>sessionStorage.setItem('anvil-agents-desktop.accessToken','ui-contract-refreshed-not-a-token'));
  console.log('PASS token_expired activity reconnect uses refreshed stored session without App rerender');




  await openHistory('thread-1');
  await visible('Fixture coordination response');
  assert.equal(await page.getByText('Fixture second conversation response',{exact:true}).count(),0);
  await openAgent('agent-beta');
  await visible('Fixture second conversation response');
  assert.equal(await page.getByText('Fixture coordination response',{exact:true}).count(),0);
  await page.getByLabel('Message',{exact:true}).fill('Unsent beta draft');
  await openHistory('thread-1');
  await visible('Fixture coordination response');
  await page.getByLabel('Message',{exact:true}).fill('Unsent alpha draft');
  await openAgent('agent-beta');
  await draftVisible('Unsent beta draft');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Unsent beta draft');
  await page.reload();
  await visible('Fixture second conversation response');
  await draftVisible('Unsent beta draft');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Unsent beta draft');
  await newConversation();
  await page.getByLabel('Agent',{exact:true}).selectOption('');
  await page.getByLabel('Remote harness').selectOption('agy-review');
  await page.getByLabel('Message',{exact:true}).fill('Unsent new AGY conversation');
  await openHistory('thread-1');
  await draftVisible('Unsent alpha draft');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Unsent alpha draft');
  await harnessChat();
  await draftVisible('Unsent new AGY conversation');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Unsent new AGY conversation');
  assert.equal(await page.getByLabel('Remote harness').inputValue(),'agy-review');
  await page.reload();
  await page.getByLabel('Remote harness').waitFor({state:'visible'});
  await draftVisible('Unsent new AGY conversation');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Unsent new AGY conversation');
  assert.equal(await page.getByLabel('Remote harness').inputValue(),'agy-review');
  console.log('PASS independent thread drafts and unfinished new-conversation configuration survive sidebar/reload');
  await page.getByLabel('Project',{exact:true}).waitFor({state:'visible'});
  assert.deepEqual(await page.getByLabel('Project',{exact:true}).locator('option').allTextContents(),['Anvil','Hazy Trade']);
  await namespace('hazy-trade');
  assert.equal(await page.getByRole('button',{name:'Agent alpha Standing conversation',exact:true}).count(),0);
  await newConversation();
  await page.getByLabel('Agent',{exact:true}).selectOption('agent-trade');
  assert.equal(await page.getByText('Fixture second conversation response',{exact:true}).count(),0);
  await page.getByLabel('Message',{exact:true}).fill('Trade namespace draft');
  await namespace('anvilhub');
  await page.getByLabel('Remote harness').waitFor({state:'visible'});
  await draftVisible('Unsent new AGY conversation');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Unsent new AGY conversation');
  await openAgent('agent-beta');
  await namespace('hazy-trade');
  await configuration();
  await page.getByLabel('Agent',{exact:true}).waitFor({state:'visible'});
  await draftVisible('Trade namespace draft');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Trade namespace draft');
  await namespace('anvilhub');
  await visible('Fixture second conversation response');
  await draftVisible('Unsent beta draft');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Unsent beta draft');
  await namespace('hazy-trade');
  await configuration();
  await page.getByLabel('Agent',{exact:true}).waitFor({state:'visible'});
  await page.getByLabel('Message',{exact:true}).fill('Namespace isolated fixture');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('status').filter({hasText:'Message received; waiting for the agent to start'}).waitFor();
  assert.equal(creates.at(-1).ns,'hazy-trade');
  assert.deepEqual(errors,[]);
  console.log('PASS two-thread isolation and namespace switch; no browser runtime errors');
  await newConversation();
  await page.getByLabel('Agent',{exact:true}).selectOption('');
  await page.getByLabel('Remote harness').selectOption('codex-standard');
  await page.getByLabel('Message',{exact:true}).fill('Harness-only fixture');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('status').filter({hasText:'Message received; waiting for the agent to start'}).waitFor();
  assert.equal(creates.at(-1).profileName,undefined);
  assert.equal(creates.at(-1).harnessProfileName,'codex-standard');
  console.log('PASS standalone harness conversation; explicit manager peer selection survives reload');
  complete('thread-4','Fixture harness-only response');
  await visible('Fixture harness-only response');
  await page.getByLabel('Message',{exact:true}).fill('Draft for unavailable conversation');
  deniedDetails.set('thread-4',404);
  await page.getByRole('alert').filter({hasText:'Fixture access denied'}).waitFor();
  const deniedCount=detailGets.get('thread-4');
  await page.clock.fastForward(10001);
  assert.equal(detailGets.get('thread-4'),deniedCount);
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Draft for unavailable conversation');
  assert.equal(await page.getByRole('button',{name:'Send',exact:true}).isDisabled(),true);
  await page.reload();
  await page.getByRole('alert').filter({hasText:'Fixture access denied'}).waitFor();
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Draft for unavailable conversation');
  await newConversation();
  assert.equal(await page.getByLabel('Message',{exact:true}).isDisabled(),false);
  console.log('PASS denied404 stops polling, preserves draft on reload, and allows a new conversation');
  await namespace('anvilhub');
  await openAgent('agent-alpha');
  assert.equal(await page.getByText('Fixture coordination response',{exact:true}).count(),0);
  const alphaID=standing.get('anvilhub/agent-alpha');
  await page.getByLabel('Message',{exact:true}).fill('Canonical standing identity');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('button',{name:'Waiting for reply…'}).waitFor();
  complete(alphaID,'Fixture standing brain reply');
  await visible('Fixture standing brain reply');
  await page.reload(); await visible('Fixture standing brain reply');
  await openAgent('agent-beta'); await openAgent('agent-alpha'); await visible('Fixture standing brain reply');
  assert.equal(standing.get('anvilhub/agent-alpha'),alphaID);
  assert.equal(await page.locator('.agent-row').count(),2);
  await openHistory('thread-1'); await visible('Fixture coordination response');
  assert.equal(await page.getByLabel('Remote harness').inputValue(),'agy-review');
  await openAgent('agent-alpha'); await visible('Fixture standing brain reply');
  console.log('PASS roster lists agent identities; standing conversation survives reload and history keeps its configured harness');

  await page.getByLabel('Message',{exact:true}).fill('Draft retained when another agent cannot open');
  standingFailures.add('anvilhub/agent-beta');
  await page.locator('.agent-row[title="agent-beta"]').click();
  await page.getByRole('alert').filter({hasText:'Fixture agent could not be opened'}).waitFor();
  await draftVisible('Draft retained when another agent cannot open');
  await visible('Fixture standing brain reply');
  assert.equal(await page.getByLabel('Agent',{exact:true}).inputValue(),'agent-alpha');
  assert.equal(await page.getByRole('button',{name:'Send',exact:true}).isEnabled(),true);
  standingFailures.delete('anvilhub/agent-beta');
  console.log('PASS failed agent opening retains prior identity, history and editable draft');

  let releaseAgent;
  standingGates.set('anvilhub/agent-alpha',new Promise(resolve=>{releaseAgent=resolve;}));
  const slowEnsure=page.waitForRequest(r=>r.method()==='POST' && r.postDataJSON()?.standing && r.postDataJSON()?.profileName==='agent-alpha');
  await page.locator('.agent-row[title="agent-alpha"]').click(); await slowEnsure;
  await openAgent('agent-beta');
  releaseAgent(); standingGates.delete('anvilhub/agent-alpha');
  await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));
  assert.equal(await page.getByLabel('Agent',{exact:true}).inputValue(),'agent-beta');
  await visible('Fixture second conversation response');
  await openAgent('agent-alpha'); await draftVisible('Draft retained when another agent cannot open');
  assert.equal(standing.get('anvilhub/agent-alpha'),alphaID);
  console.log('PASS rapid agent switching ignores late canonical responses without losing standing drafts');
  const canonical=threads.get(alphaID);
  for (const [index,metadata] of [
    {kind:'legacy_output_unavailable',backend:'hermesAgent',runName:'legacy-hermes-run'},
    {kind:'internal_diagnostic',backend:'hermesAgent'},
    {kind:'legacy_output_unavailable',backend:'codex'},
  ].entries()) canonical.messages.push({id:`safe-system-${index}`,threadId:alphaID,role:'system',content:`PRIVATE_SYSTEM_CONTENT_${index}`,metadata,sequence:canonical.messages.length+1,createdAt:new Date().toISOString()});
  const legacyNotice=page.getByRole('complementary',{name:'Earlier reply unavailable'});
  await legacyNotice.waitFor({state:'visible'});
  assert.equal(await legacyNotice.count(),1);
  assert.equal(await legacyNotice.locator('.chat-bubble').count(),0);
  assert.equal(await page.getByText(/PRIVATE_SYSTEM_CONTENT_/).count(),0);
  await visible('Fixture standing brain reply');
  console.log('PASS legacy Hermes reply gets neutral unavailable notice without exposing arbitrary system content');
  const completedRun=canonical.turns.at(-1).runName;
  const completedReads=eventGets.get(completedRun)||0;
  const completedOutput=page.waitForResponse(r=>r.url().includes(`/agent-runs/${completedRun}/events`));
  await page.getByText('Runner activity',{exact:true}).click();
  await completedOutput;
  assert.equal(eventGets.get(completedRun),completedReads+1);
  await page.getByText('Runner activity',{exact:true}).click();
  const debugMarker='EXPLICIT_HERMES_REASONING_DEBUG_FIXTURE';
  eventPlans.set('legacy-hermes-run',[{body:event('log',{line:debugMarker})+event('complete',{})}]);
  assert.equal(eventGets.get('legacy-hermes-run'),undefined);
  assert.equal(await page.getByText(debugMarker,{exact:true}).count(),0);
  await legacyNotice.getByRole('button',{name:'View original runner output'}).click();
  await page.getByText('Reasoning and runner output',{exact:true}).waitFor();
  await visible(debugMarker);
  assert.equal(eventGets.get('legacy-hermes-run'),1);
  assert.equal(await page.locator('.chat-bubble').filter({hasText:debugMarker}).count(),0);
  await page.getByText('Reasoning and runner output',{exact:true}).click();
  // Native details closes before its queued toggle event updates React. Wait
  // for unmount as well as hiding: closing must stop the raw log subscriber.
  await page.getByText(debugMarker,{exact:true}).waitFor({state:'hidden'});
  await page.getByText(debugMarker,{exact:true}).waitFor({state:'detached'});
  assert.equal(await page.getByText(debugMarker,{exact:true}).count(),0);
  console.log('PASS completed turns and legacy Hermes reasoning stay inspectable only in explicitly opened authenticated runner output');
  const queuedSince=new Date(Date.now()-73*60*1000).toISOString();
  const delayedRun='fixture-delayed-controller-run';
  const delayedTurn={id:'delayed-turn',requestId:'delayed-request',userMessageId:'delayed-user',status:'queued',runName:delayedRun,createdAt:queuedSince};
  canonical.turns.push(delayedTurn);canonical.activeTurn=delayedTurn;
  canonical.messages.push({id:'delayed-user',threadId:alphaID,role:'user',content:'Saved while the controller is unavailable',sequence:canonical.messages.length+1,createdAt:queuedSince});
  eventPlans.set(delayedRun,[{body:event('snapshot',{run:{phase:'Pending'}})+event('complete',{})}]);
  const postsBeforeDelay=posts.length;
  await visible('Agent startup is delayed; no harness work has been reported');
  assert.equal(await page.locator('.turn-activity-delayed').count(),1);
  await page.getByLabel('Message',{exact:true}).fill('Draft while startup is delayed');
  assert.equal(await page.getByRole('button',{name:'Waiting for reply…'}).isDisabled(),true);
  assert.equal(await page.getByRole('button',{name:/^(Cancel|Retry|Reset)$/}).count(),0);
  await page.reload();
  await visible('Agent startup is delayed; no harness work has been reported');
  await draftVisible('Draft while startup is delayed');
  assert.equal(posts.length,postsBeforeDelay);
  canonical.recoveryPending=true;
  await visible('Cannot check agent progress right now. Your message is saved.');
  assert.equal(await page.locator('.turn-activity-delayed').count(),1);
  assert.equal(await page.getByRole('button',{name:'Waiting for reply…'}).isDisabled(),true);
  canonical.recoveryPending=false;
  const noLaunchError='This chat turn did not start within 5 minutes. No runner Job was found. Send a new message to try again.';
  complete(alphaID,noLaunchError,true);
  await page.locator('.banner-error').filter({hasText:noLaunchError}).waitFor();
  await draftVisible('Draft while startup is delayed');
  assert.equal(await page.getByRole('button',{name:'Send',exact:true}).isEnabled(),true);
  assert.equal(posts.length,postsBeforeDelay);
  console.log('PASS restored73-minute queue and unavailable recovery are visibly delayed, preserving draft and locks until server terminal status');
  loseNextReceipt=true;
  await page.getByRole('button',{name:'Retry last message',exact:true}).click();
  await page.getByRole('alert').filter({hasText:'Fixture accepted receipt was lost'}).waitFor();
  assert.equal(posts.at(-1).content,'Saved while the controller is unavailable');
  const retryID=posts.at(-1).requestId;
  assert.notEqual(retryID,delayedTurn.requestId);
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Draft while startup is delayed');
  assert.equal(await page.getByRole('button',{name:'Send',exact:true}).isDisabled(),true);
  await page.reload();
  await draftVisible('Draft while startup is delayed');
  await page.getByRole('button',{name:'Check retry delivery',exact:true}).click();
  await page.getByRole('button',{name:'Waiting for reply…'}).waitFor();
  assert.equal(posts.at(-1).requestId,retryID);
  assert.equal(posts.length,postsBeforeDelay+2);
  assert.equal(canonical.turns.filter(t=>t.requestId===retryID).length,1);
  assert.equal(delayedTurn.status,'failed');
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Draft while startup is delayed');
  complete(alphaID,'Reply from the safely retried original message');
  await visible('Reply from the safely retried original message');
  await draftVisible('Draft while startup is delayed');
  assert.equal(await page.getByRole('button',{name:'Send',exact:true}).isEnabled(),true);
  console.log('PASS confirmed no-launch retry resends matching original with fresh key, preserves draft, and survives lost receipt/reload without duplicate execution');
  const postsBeforeManager = posts.length;
  const manager = profile('anvil-primaris-agent-manager');
  const specialist = profile('aster-book-manager');
  projectRosters.set('anvilhub',[profile('desktop-assistant'),profile('agent-alpha'),manager,specialist]);
  await page.reload();
  await draftVisible('Draft while startup is delayed');
  assert.equal(await page.locator('.agent-row-selected').getAttribute('title'),'agent-alpha');
  assert.equal(await page.getByRole('region',{name:'Project manager',exact:true}).locator('.agent-row').getAttribute('title'),manager.metadata.name);
  assert.equal(await page.getByRole('region',{name:'Agents',exact:true}).locator('.agent-row[title="aster-book-manager"]').count(),1);
  assert.equal(await page.locator('.agent-role-badge').count(),0);
  // First visits prefer the designated manager; saved selections above win.
  await page.evaluate(()=>{
    sessionStorage.removeItem('anvil-agents-desktop.chat.selected.'+JSON.stringify(['anvilhub','']));
    sessionStorage.removeItem('anvil-agents-desktop.chat.new-config.'+JSON.stringify(['anvilhub','']));
  });
  await page.reload(); await draftVisible('');
  assert.equal(await page.locator('.agent-row-selected').getAttribute('title'),manager.metadata.name);
  assert.equal(await page.locator('.agent-role-badge').textContent(),'Project manager');
  await configuration();
  assert.equal(await page.getByLabel('Role',{exact:true}).inputValue(),'persona');
  assert.equal(await page.getByLabel('Allow this agent to delegate messages',{exact:true}).isChecked(),false);
  await openAgent('agent-alpha'); await draftVisible('Draft while startup is delayed');
  // Explicit metadata supersedes the legacy identity, regardless of its name.
  const custom = profile('project-guide');
  custom.metadata.labels={'control.anvil.hazyforge.io/chat-role':'project-manager'};
  projectRosters.set('anvilhub',[profile('agent-alpha'),manager,custom,specialist]);
  await page.reload(); await draftVisible('Draft while startup is delayed');
  assert.equal(await page.getByRole('region',{name:'Project manager',exact:true}).locator('.agent-row').getAttribute('title'),'project-guide');
  assert.equal(await page.getByRole('region',{name:'Agents',exact:true}).locator('.agent-row[title="anvil-primaris-agent-manager"]').count(),1);
  await openAgent('project-guide'); await draftVisible('');
  assert.equal(await page.locator('.agent-role-badge').textContent(),'Project manager');
  assert.equal(posts.length,postsBeforeManager);
  assert.deepEqual(errors,[]);
  console.log('PASS manager grouping, exact legacy designation, explicit override, first-visit default, retained drafts, and unchanged delegation authority');
  await page.getByLabel('Message',{exact:true}).fill('Recovery fixture message');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('button',{name:'Waiting for reply…'}).waitFor();
  const recovering=threads.get(posts.at(-1).thread);
  const recoveryTurn=recovering.activeTurn;
  recoveryTurn.status='running';
  await page.waitForTimeout(2200);
  const oldAttempt=structuredClone(recovering);
  const originalRun=recoveryTurn.runName;
  recoveryTurn.retryCount=1;
  recoveryTurn.status='queued';
  recoveryTurn.retryAt=new Date(Date.now()+30000).toISOString();
  recoveryTurn.recoveryReason='The previous runner did not start.';
  recoveryTurn.attempts=[{runName:originalRun,error:'Startup deadline reached before launch',completedAt:new Date().toISOString()}];
  recoveryTurn.runName=originalRun+'-retry-1';
  await page.getByRole('status').filter({hasText:'attempt 2 of 3'}).waitFor();
  await page.getByLabel('Message',{exact:true}).fill('Unsent draft during automatic recovery');
  const recoveryPosts=posts.length;
  withheldDetails.set(recovering.id,oldAttempt);
  await page.waitForTimeout(2200);
  assert.match(await page.locator('.turn-activity [role="status"]').textContent(),/attempt 2 of 3/);
  withheldDetails.delete(recovering.id);
  await page.reload(); await draftVisible('Unsent draft during automatic recovery');
  await page.getByRole('status').filter({hasText:'attempt 2 of 3'}).waitFor();
  assert.equal(await page.getByRole('button',{name:'Waiting for reply…'}).isDisabled(),true);
  assert.equal(recovering.messages.filter(m=>m.role==='user').length,1);
  assert.equal(posts.length,recoveryPosts);
  complete(recovering.id,'Reply after automatic startup recovery');
  await visible('Reply after automatic startup recovery');
  await draftVisible('Unsent draft during automatic recovery');
  assert.equal(await page.getByRole('button',{name:'Send',exact:true}).isEnabled(),true);
  assert.deepEqual(errors,[]);
  console.log('PASS automatic recovery advances attempts, rejects old snapshots, survives reload, preserves one message and draft, and releases lock after reply');
  await page.getByLabel('Message',{exact:true}).fill('Retry this after fixing startup configuration');
  await page.getByRole('button',{name:'Send',exact:true}).click();
  await page.getByRole('button',{name:'Waiting for reply…'}).waitFor();
  const manuallyRetried=threads.get(posts.at(-1).thread);
  const failedRequest=posts.at(-1).requestId;
  complete(manuallyRetried.id,'Fixture configuration blocked startup',true);
  await page.getByRole('button',{name:'Retry last message',exact:true}).waitFor();
  const postsBeforeManual=posts.length;
  await page.getByLabel('Message',{exact:true}).fill('Draft retained during explicit retry');
  await page.getByRole('button',{name:'Retry last message',exact:true}).click();
  await page.getByRole('button',{name:'Waiting for reply…'}).waitFor();
  assert.equal(posts.length,postsBeforeManual+1);
  assert.equal(posts.at(-1).content,'Retry this after fixing startup configuration');
  assert.notEqual(posts.at(-1).requestId,failedRequest);
  assert.equal(await page.getByLabel('Message',{exact:true}).inputValue(),'Draft retained during explicit retry');
  complete(manuallyRetried.id,'Reply after explicit configuration recovery');
  await visible('Reply after explicit configuration recovery');
  assert.deepEqual(errors,[]);
  console.log('PASS explicit retry is available for other terminal failures, sends saved content only on click and preserves draft');
  const repairedMessage = manuallyRetried.messages.at(-1);
  repairedMessage.metadata = {backend:'openClaw',replyFormat:'openclaw.payloads/v1',runName:'original-recovered-run'};
  await page.reload();
  await visible('Reply after explicit configuration recovery');
  const beforeRepair = structuredClone(manuallyRetried);
  Object.assign(beforeRepair.messages.at(-1), {role:'system',content:'older reply unavailable',metadata:{backend:'openClaw',kind:'legacy_output_unavailable',runName:'original-recovered-run'}});
  withheldDetails.set(manuallyRetried.id,beforeRepair);
  await page.waitForTimeout(4400);
  await visible('Reply after explicit configuration recovery');
  withheldDetails.delete(manuallyRetried.id);
  await page.reload();
  await visible('Reply after explicit configuration recovery');
  await draftVisible('Draft retained during explicit retry');
  assert.equal(posts.length,postsBeforeManual+1);
  console.log('PASS recovered OpenClaw answer survives stale unavailable refresh, reload, and retains draft without sending another message');
  config.chat.enabled=false;
  await page.reload();
  await visible('Remote chat is not enabled on this server.');
  assert.equal(await page.getByLabel('Message',{exact:true}).isDisabled(),true);
  assert.equal(await page.getByRole('button',{name:'Send',exact:true}).isDisabled(),true);
  console.log('PASS server-disabled chat cannot submit');
  console.log('UI CONTRACT TESTS PASSED — fixture responses are not remote model evidence');
} finally {await context.close();await browser.close();await new Promise(resolve=>server.close(resolve));}
