---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: volumeprofiles.control.anvil.hazyforge.io
spec:
  group: control.anvil.hazyforge.io
  names:
    kind: VolumeProfile
    listKind: VolumeProfileList
    plural: volumeprofiles
    shortNames:
    - volprof
    singular: volumeprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.volumeCount
      name: Volumes
      type: integer
    - jsonPath: .status.totalRequestedStorage
      name: Requested
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              applicationRef:
                description: ApplicationRef optionally scopes this profile to one
                  opaque application key.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              capacityPolicy:
                description: |-
                  CapacityPolicy records placement and sizing intent. It does not replace
                  concrete PVC storage requests.
                properties:
                  maxNodeAllocatableEphemeralStoragePercent:
                    description: |-
                      MaxNodeAllocatableEphemeralStoragePercent is an operator guardrail for
                      local-path-backed grouped PVCs. Kubernetes PVCs still receive concrete
                      storage requests.
                    format: int32
                    maximum: 90
                    minimum: 1
                    type: integer
                type: object
              description:
                description: Description documents the reusable storage shape in human
                  terms.
                type: string
              notes:
                description: Notes documents operational intent, migration, or cleanup
                  expectations.
                type: string
              volumes:
                description: Volumes are named reusable volume entries.
                items:
                  properties:
                    accessModes:
                      description: AccessModes are the default PVC access modes.
                      items:
                        type: string
                      type: array
                    externalSync:
                      description: |-
                        ExternalSync declares reusable sync intent for this volume entry. Concrete
                        volumes may inherit, override, or disable it.
                      properties:
                        credentialsSecretRef:
                          description: |-
                            CredentialsSecretRef references a namespace-local Secret containing
                            provider credentials. The operator does not read the Secret in this slice.
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        direction:
                          description: Direction documents the intended data-flow
                            direction.
                          enum:
                          - SeedOnly
                          - SyncBack
                          - Bidirectional
                          type: string
                        disabled:
                          description: |-
                            Disabled lets a concrete volume explicitly turn off sync inherited from a
                            reusable profile.
                          type: boolean
                        notes:
                          description: Notes documents migration, retention, or operator
                            expectations.
                          type: string
                        provider:
                          description: Provider identifies the external sync backend.
                          enum:
                          - s3
                          type: string
                        s3:
                          description: S3 describes the object-store location when
                            provider=s3.
                          properties:
                            bucket:
                              description: Bucket is the target S3-compatible bucket
                                name.
                              type: string
                            endpoint:
                              description: Endpoint optionally selects an S3-compatible
                                endpoint.
                              type: string
                            prefix:
                              description: Prefix is the object key prefix within
                                the bucket.
                              type: string
                            region:
                              description: Region is the provider region hint.
                              type: string
                          type: object
                        schedule:
                          description: |-
                            Schedule is an implementation hint for future periodic sync workers, such
                            as a cron expression or interval string. It is not executed today.
                          type: string
                        seedOnCreate:
                          description: |-
                            SeedOnCreate requests initial volume population from the external source.
                            It is recorded as intent only in this API slice.
                          type: boolean
                        syncBack:
                          description: |-
                            SyncBack requests copying local volume changes back to the external
                            source. It is recorded as intent only in this API slice.
                          type: boolean
                      type: object
                    extraEnv:
                      description: |-
                        ExtraEnv provides non-secret path environment defaults for AgentRuns that
                        attach an AgentDataVolume inheriting this profile entry.
                      items:
                        description: |-
                          AgentDataVolumePathEnvVar exposes only a named absolute state path. It is
                          intentionally narrower than corev1.EnvVar so storage metadata cannot select
                          Secrets or inject general runner configuration.
                        properties:
                          name:
                            description: Name identifies a backend home, state, cache,
                              config, or data directory.
                            pattern: ^([A-Z][A-Z0-9_]*_)?(HOME|STATE_DIR|CACHE_DIR|CONFIG_DIR|DATA_DIR)$
                            type: string
                          value:
                            description: Value is an absolute path under the volume's
                              mountPath.
                            pattern: ^/
                            type: string
                        required:
                        - name
                        - value
                        type: object
                      type: array
                    mountPath:
                      description: |-
                        MountPath is the default container path for consumers that inherit this
                        volume profile entry.
                      type: string
                    name:
                      description: Name is the stable profile entry name.
                      minLength: 1
                      type: string
                    nodeSelector:
                      additionalProperties:
                        type: string
                      description: |-
                        NodeSelector describes required placement for local-path or host-local
                        storage.
                      type: object
                    notes:
                      description: Notes documents operational intent, migration,
                        or cleanup expectations.
                      type: string
                    purpose:
                      description: |-
                        Purpose describes how this volume should be used, such as cargo-cache,
                        target-cache, workspace, or agent-home.
                      type: string
                    size:
                      description: Size records concrete and advisory sizing intent.
                      properties:
                        maxNodeAllocatableEphemeralStoragePercent:
                          description: |-
                            MaxNodeAllocatableEphemeralStoragePercent is an advisory per-entry
                            percentage guardrail. It is not directly applied to Kubernetes PVCs.
                          format: int32
                          maximum: 90
                          minimum: 1
                          type: integer
                        request:
                          anyOf:
                          - type: integer
                          - type: string
                          description: |-
                            Request is the concrete PVC storage request to apply when this entry is
                            used as a default.
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                      type: object
                    storageClassName:
                      description: StorageClassName is the default StorageClass for
                        concrete PVCs.
                      type: string
                    subPath:
                      description: SubPath optionally mounts only one subdirectory
                        of the claim by default.
                      type: string
                  required:
                  - name
                  type: object
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            required:
            - volumes
            type: object
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastError:
                description: LastError summarizes the current blocking validation
                  error, when any.
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              totalRequestedStorage:
                description: |-
                  TotalRequestedStorage is the sum of concrete spec.volumes[].size.request
                  values.
                type: string
              volumeCount:
                description: VolumeCount is the number of profile entries observed
                  by the controller.
                format: int32
                type: integer
              volumes:
                items:
                  properties:
                    externalSync:
                      properties:
                        conditions:
                          items:
                            description: Condition contains details for one aspect
                              of the current state of this API Resource.
                            properties:
                              lastTransitionTime:
                                description: |-
                                  lastTransitionTime is the last time the condition transitioned from one status to another.
                                  This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                                format: date-time
                                type: string
                              message:
                                description: |-
                                  message is a human readable message indicating details about the transition.
                                  This may be an empty string.
                                maxLength: 32768
                                type: string
                              observedGeneration:
                                description: |-
                                  observedGeneration represents the .metadata.generation that the condition was set based upon.
                                  For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                  with respect to the current state of the instance.
                                format: int64
                                minimum: 0
                                type: integer
                              reason:
                                description: |-
                                  reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                  Producers of specific condition types may define expected values and meanings for this field,
                                  and whether the values are considered a guaranteed API.
                                  The value should be a CamelCase string.
                                  This field may not be empty.
                                maxLength: 1024
                                minLength: 1
                                pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                                type: string
                              status:
                                description: status of the condition, one of True,
                                  False, Unknown.
                                enum:
                                - "True"
                                - "False"
                                - Unknown
                                type: string
                              type:
                                description: type of condition in CamelCase or in
                                  foo.example.com/CamelCase.
                                maxLength: 316
                                pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                                type: string
                            required:
                            - lastTransitionTime
                            - message
                            - reason
                            - status
                            - type
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - type
                          x-kubernetes-list-type: map
                        direction:
                          type: string
                        lastAttemptTime:
                          format: date-time
                          type: string
                        lastError:
                          type: string
                        lastSuccessTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        phase:
                          type: string
                        provider:
                          type: string
                      type: object
                    name:
                      type: string
                    purpose:
                      type: string
                    requestedStorage:
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
