---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: agentauthsessions.control.anvil.hazyforge.io
spec:
  group: control.anvil.hazyforge.io
  names:
    kind: AgentAuthSession
    listKind: AgentAuthSessionList
    plural: agentauthsessions
    shortNames:
    - agauth
    singular: agentauthsession
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.action
      name: Action
      type: string
    - jsonPath: .spec.dataVolumeRef.name
      name: Volume
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AgentAuthSessionSpec is the immutable intent for one auth maintenance operation.
              Credential bytes never appear in the spec; reauth mounts a short-lived staging Secret.
            properties:
              action:
                description: Action selects whether to import fresh credentials or
                  clear durable login state.
                enum:
                - reauth
                - logout
                type: string
              bootstrapSecretKey:
                description: |-
                  BootstrapSecretKey is the key written into the bootstrap Secret. Defaults to CODEX_AUTH_JSON
                  for codex and GROK_AUTH_JSON for grokBuild.
                type: string
              bootstrapSecretRef:
                description: |-
                  BootstrapSecretRef optionally names the durable seed Secret updated after successful reauth.
                  The controller refuses ExternalSecret-managed targets.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              dataVolumeRef:
                description: DataVolumeRef names the same-namespace AgentDataVolume
                  whose PVC holds the durable home.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              provider:
                description: Provider selects the harness-specific durable auth layout.
                enum:
                - codex
                - grokBuild
                type: string
              seedID:
                description: |-
                  SeedID is an opaque marker written next to durable auth.json so runners can reseed only
                  when operators deliberately change the seed. Required for reauth.
                maxLength: 128
                pattern: ^[A-Za-z0-9._:-]+$
                type: string
              stagingSecretRef:
                description: |-
                  StagingSecretRef names a same-namespace Secret that holds provider auth bytes for reauth.
                  Codex expects CODEX_AUTH_JSON; grokBuild expects GROK_AUTH_JSON. The controller mounts it
                  read-only and deletes it after a successful reauth when it is session-owned.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              timeoutSeconds:
                description: TimeoutSeconds bounds the maintenance Job active deadline.
                  Defaults to 300.
                format: int32
                maximum: 3600
                minimum: 30
                type: integer
            required:
            - action
            - dataVolumeRef
            - provider
            type: object
            x-kubernetes-validations:
            - message: AgentAuthSession spec is immutable; create a new session instead
              rule: self == oldSelf
            - message: spec.stagingSecretRef is required when action is reauth
              rule: self.action != 'reauth' || (has(self.stagingSecretRef) && has(self.stagingSecretRef.name)
                && size(self.stagingSecretRef.name) > 0)
            - message: spec.seedID is required when action is reauth
              rule: self.action != 'reauth' || (has(self.seedID) && size(self.seedID)
                > 0)
          status:
            description: AgentAuthSessionStatus records verified volume identity,
              Job progress, and terminal outcome.
            properties:
              activeConsumerRuns:
                items:
                  type: string
                type: array
              claimRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              claimUID:
                type: string
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dataVolumeRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              dataVolumeUID:
                type: string
              jobRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              jobUID:
                type: string
              lastError:
                type: string
              mountPath:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                allOf:
                - enum:
                  - Pending
                  - WaitingForIdle
                  - Running
                  - Succeeded
                  - Failed
                - enum:
                  - Pending
                  - WaitingForIdle
                  - Running
                  - Succeeded
                  - Failed
                description: AgentAuthSessionPhase is the controller-reported lifecycle
                  of one auth maintenance session.
                type: string
              seedID:
                type: string
              startedAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
