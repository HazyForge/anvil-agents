---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: agentdatavolumecopies.control.anvil.hazyforge.io
spec:
  group: control.anvil.hazyforge.io
  names:
    kind: AgentDataVolumeCopy
    listKind: AgentDataVolumeCopyList
    plural: agentdatavolumecopies
    shortNames:
    - agdvcopy
    singular: agentdatavolumecopy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.sourceRef.name
      name: Source
      type: string
    - jsonPath: .spec.destination.name
      name: Destination
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentDataVolumeCopySpec is the immutable intent for one volume
              copy operation.
            properties:
              allowNonEmptyDestination:
                description: |-
                  AllowNonEmptyDestination permits overwriting a destination that already has files.
                  Default false: destination root must be empty (or only lost+found).
                type: boolean
              destination:
                description: Destination describes the target AgentDataVolume and
                  node placement.
                properties:
                  backend:
                    description: Backend overrides the destination backend label.
                      Empty inherits the source.
                    enum:
                    - codex
                    - openCode
                    - hermesAgent
                    - openClaw
                    - grokBuild
                    - piAgent
                    - custom
                    type: string
                  name:
                    description: Name is the same-namespace destination AgentDataVolume.
                      Created when missing.
                    maxLength: 253
                    minLength: 1
                    type: string
                  nodeSelector:
                    additionalProperties:
                      type: string
                    description: |-
                      NodeSelector pins destination pods and local-path first-consumer binding.
                      Required for Stream copies so WaitForFirstConsumer binds on the intended node.
                    minProperties: 1
                    type: object
                  notes:
                    description: Notes are stored on the destination AgentDataVolume
                      when the controller creates it.
                    type: string
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      Size overrides the source size when creating the destination claim.
                      Empty inherits the source size (or status capacity when size is unset).
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: |-
                      StorageClassName overrides the source when creating the destination claim.
                      Empty inherits the source storage class.
                    type: string
                required:
                - name
                - nodeSelector
                type: object
              method:
                default: Stream
                description: Method selects the transfer strategy. Defaults to Stream.
                enum:
                - Stream
                type: string
              sourceRef:
                description: SourceRef names the same-namespace AgentDataVolume to
                  copy from.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              timeoutSeconds:
                description: TimeoutSeconds bounds the combined transfer Jobs. Defaults
                  to 1800.
                format: int32
                maximum: 14400
                minimum: 60
                type: integer
              verify:
                default: true
                description: |-
                  Verify runs a post-copy file-count check between source and destination.
                  Defaults to true.
                type: boolean
            required:
            - destination
            - sourceRef
            type: object
            x-kubernetes-validations:
            - message: AgentDataVolumeCopy spec is immutable; create a new copy instead
              rule: self == oldSelf
            - message: source and destination AgentDataVolume names must differ
              rule: self.sourceRef.name != self.destination.name
          status:
            description: AgentDataVolumeCopyStatus records progress and terminal outcome.
            properties:
              activeConsumerRuns:
                items:
                  type: string
                type: array
              bytesCopiedHint:
                type: string
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              destinationClaimRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              destinationClaimUID:
                type: string
              destinationJobRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              destinationJobUID:
                type: string
              destinationNode:
                type: string
              destinationRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              destinationUID:
                type: string
              fileCountDest:
                format: int64
                type: integer
              fileCountSource:
                format: int64
                type: integer
              lastError:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                allOf:
                - enum:
                  - Pending
                  - WaitingForIdle
                  - PreparingDestination
                  - Streaming
                  - Verifying
                  - Succeeded
                  - Failed
                - enum:
                  - Pending
                  - WaitingForIdle
                  - PreparingDestination
                  - Streaming
                  - Verifying
                  - Succeeded
                  - Failed
                description: AgentDataVolumeCopyPhase is the controller-reported lifecycle
                  of one volume copy.
                type: string
              serviceRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              sourceClaimRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              sourceClaimUID:
                type: string
              sourceJobRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              sourceJobUID:
                type: string
              sourceNode:
                type: string
              sourceRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              sourceUID:
                type: string
              startedAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
