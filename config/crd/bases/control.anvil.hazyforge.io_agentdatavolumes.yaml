---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: agentdatavolumes.control.anvil.hazyforge.io
spec:
  group: control.anvil.hazyforge.io
  names:
    kind: AgentDataVolume
    listKind: AgentDataVolumeList
    plural: agentdatavolumes
    shortNames:
    - agdv
    singular: agentdatavolume
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.backend
      name: Backend
      type: string
    - jsonPath: .status.claimRef.name
      name: Claim
      type: string
    - jsonPath: .status.volumeName
      name: Volume
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              accessModes:
                description: |-
                  AccessModes are used when creating the backing claim. Empty defaults to
                  ReadWriteOnce.
                items:
                  type: string
                type: array
              agentName:
                description: AgentName is the stable logical agent identity this storage
                  belongs to.
                type: string
              applicationRef:
                description: |-
                  ApplicationRef scopes this durable home to one opaque application key. New
                  application-scoped volumes must set it; legacy unscoped volumes remain
                  readable during migration.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              backend:
                description: |-
                  Backend documents the intended AgentRun backend for this durable home.
                  The controller does not require an exact match so data can be migrated,
                  but AgentRun prompts and operator UIs should show it.
                enum:
                - codex
                - openCode
                - hermesAgent
                - openClaw
                - grokBuild
                - piAgent
                - custom
                type: string
              claimName:
                description: |-
                  ClaimName selects or creates the backing PersistentVolumeClaim. Empty
                  defaults to agent-data-<AgentDataVolume name>.
                type: string
              externalSync:
                description: |-
                  ExternalSync declares future object-store sync for this concrete volume.
                  It can inherit defaults from ProfileRef/ProfileVolumeName and can disable
                  inherited sync with disabled=true. The v1alpha1 controller records status
                  only and does not move data.
                properties:
                  credentialsSecretRef:
                    description: |-
                      CredentialsSecretRef references a namespace-local Secret containing
                      provider credentials. The operator does not read the Secret in this slice.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  direction:
                    description: Direction documents the intended data-flow direction.
                    enum:
                    - SeedOnly
                    - SyncBack
                    - Bidirectional
                    type: string
                  disabled:
                    description: |-
                      Disabled lets a concrete volume explicitly turn off sync inherited from a
                      reusable profile.
                    type: boolean
                  notes:
                    description: Notes documents migration, retention, or operator
                      expectations.
                    type: string
                  provider:
                    description: Provider identifies the external sync backend.
                    enum:
                    - s3
                    type: string
                  s3:
                    description: S3 describes the object-store location when provider=s3.
                    properties:
                      bucket:
                        description: Bucket is the target S3-compatible bucket name.
                        type: string
                      endpoint:
                        description: Endpoint optionally selects an S3-compatible
                          endpoint.
                        type: string
                      prefix:
                        description: Prefix is the object key prefix within the bucket.
                        type: string
                      region:
                        description: Region is the provider region hint.
                        type: string
                    type: object
                  schedule:
                    description: |-
                      Schedule is an implementation hint for future periodic sync workers, such
                      as a cron expression or interval string. It is not executed today.
                    type: string
                  seedOnCreate:
                    description: |-
                      SeedOnCreate requests initial volume population from the external source.
                      It is recorded as intent only in this API slice.
                    type: boolean
                  syncBack:
                    description: |-
                      SyncBack requests copying local volume changes back to the external
                      source. It is recorded as intent only in this API slice.
                    type: boolean
                type: object
              extraEnv:
                description: |-
                  ExtraEnv injects non-secret backend state paths when an AgentRun mounts
                  this volume, for example CODEX_HOME, HERMES_HOME, or OPENCLAW_STATE_DIR.
                  Credentials must still come from AgentRun envSecretRefs.
                items:
                  description: |-
                    AgentDataVolumePathEnvVar exposes only a named absolute state path. It is
                    intentionally narrower than corev1.EnvVar so storage metadata cannot select
                    Secrets or inject general runner configuration.
                  properties:
                    name:
                      description: Name identifies a backend home, state, cache, config,
                        or data directory.
                      pattern: ^([A-Z][A-Z0-9_]*_)?(HOME|STATE_DIR|CACHE_DIR|CONFIG_DIR|DATA_DIR)$
                      type: string
                    value:
                      description: Value is an absolute path under the volume's mountPath.
                      pattern: ^/
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              mountPath:
                description: |-
                  MountPath is the default container path for AgentRuns that attach this
                  data volume. Backend-specific env should point at this path or a child of
                  it.
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                description: |-
                  NodeSelector describes where pods should run when this local volume is
                  attached. AgentRuns merge these labels with their execution nodeSelector.
                type: object
              notes:
                description: |-
                  Notes documents operational intent, retention, migration, or cleanup
                  expectations for this agent home.
                type: string
              profileRef:
                description: |-
                  ProfileRef references a namespace-local VolumeProfile that provides
                  reusable defaults for this concrete agent data volume.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              profileVolumeName:
                description: |-
                  ProfileVolumeName selects one entry from the referenced VolumeProfile.
                  It may be omitted only when the profile has exactly one volume entry.
                type: string
              size:
                anyOf:
                - type: integer
                - type: string
                description: Size is used when creating the backing claim. Empty defaults
                  to 10Gi.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              storageClassName:
                description: |-
                  StorageClassName is used when creating the backing claim. Empty uses the
                  operator default when configured, otherwise the cluster default.
                type: string
              subPath:
                description: SubPath optionally mounts only one subdirectory of the
                  claim by default.
                type: string
            type: object
            x-kubernetes-validations:
            - message: spec.claimName is immutable; migrate data through a new AgentDataVolume
              rule: has(self.claimName) == has(oldSelf.claimName) && (!has(self.claimName)
                || self.claimName == oldSelf.claimName)
          status:
            properties:
              capacity:
                type: string
              claimRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              claimUID:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              externalSync:
                properties:
                  conditions:
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - type
                    x-kubernetes-list-type: map
                  direction:
                    type: string
                  lastAttemptTime:
                    format: date-time
                    type: string
                  lastError:
                    type: string
                  lastSuccessTime:
                    format: date-time
                    type: string
                  message:
                    type: string
                  phase:
                    type: string
                  provider:
                    type: string
                type: object
              extraEnv:
                items:
                  description: |-
                    AgentDataVolumePathEnvVar exposes only a named absolute state path. It is
                    intentionally narrower than corev1.EnvVar so storage metadata cannot select
                    Secrets or inject general runner configuration.
                  properties:
                    name:
                      description: Name identifies a backend home, state, cache, config,
                        or data directory.
                      pattern: ^([A-Z][A-Z0-9_]*_)?(HOME|STATE_DIR|CACHE_DIR|CONFIG_DIR|DATA_DIR)$
                      type: string
                    value:
                      description: Value is an absolute path under the volume's mountPath.
                      pattern: ^/
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              lastError:
                type: string
              mountPath:
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                type: object
              observedGeneration:
                format: int64
                type: integer
              phase:
                type: string
              profileRef:
                description: |-
                  NamespacedObjectReference identifies a Kubernetes object without importing
                  another API group. Namespace may be omitted where same-namespace resolution
                  is part of the containing resource contract.
                properties:
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                type: object
              profileVolumeName:
                type: string
              storageClassName:
                type: string
              subPath:
                type: string
              volumeName:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
