apiVersion: control.anvil.hazyforge.io/v1alpha1
kind: AgentToolSet
metadata:
  name: repository-tools
  namespace: agents
spec:
  description: Small repository inspection commands shared by agent roles.
  toolRefs:
    - name: repository-status
