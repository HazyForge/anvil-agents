apiVersion: control.anvil.hazyforge.io/v1alpha1
kind: AgentRunControl
metadata:
  name: pause-example-service
spec:
  applicationRef:
    name: example-service
  launchPolicy: Paused
  reason: Operator maintenance window
