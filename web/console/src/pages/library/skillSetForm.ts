export interface SkillSetForm { name: string; description: string; skillRefs: string[]; legacy: Record<string, unknown>; }
export function emptySkillSetForm(): SkillSetForm { return { name: "", description: "", skillRefs: [], legacy: {} }; }
function asRecord(value: unknown): Record<string, unknown> { return typeof value === "object" && value ? value as Record<string, unknown> : {}; }
export function formFromSkillSetSpec(spec: Record<string, unknown>, name = ""): SkillSetForm { return { name, description: String(spec.description ?? ""), skillRefs: Array.isArray(spec.skillRefs) ? spec.skillRefs.map((item) => String(asRecord(item).name ?? "")).filter(Boolean) : [], legacy: { ...spec } }; }
export function buildSkillSetSpec(form: SkillSetForm): Record<string, unknown> { const spec = { ...form.legacy }; const prior = Array.isArray(form.legacy.skillRefs) ? form.legacy.skillRefs.map(asRecord) : []; delete spec.description; delete spec.skillRefs; if (form.description.trim()) spec.description = form.description.trim(); if (form.skillRefs.length) spec.skillRefs = form.skillRefs.map((name) => ({ ...(prior.find((ref) => String(ref.name ?? "") === name) ?? {}), name })); return spec; }
export function validateSkillSetForm(form: SkillSetForm, isCreate: boolean): string | null { if (isCreate && !form.name.trim()) return "Name is required"; if (isCreate && !/^[a-z0-9]([-a-z0-9]*[a-z0-9])?$/.test(form.name.trim())) return "Name must be a DNS-1123 label"; return null; }
