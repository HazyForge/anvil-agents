export interface ToolSetForm { name: string; description: string; toolRefs: string[]; legacy: Record<string, unknown>; }
export function emptyToolSetForm(): ToolSetForm { return { name: "", description: "", toolRefs: [], legacy: {} }; }
function asRecord(value: unknown): Record<string, unknown> { return typeof value === "object" && value ? value as Record<string, unknown> : {}; }
export function formFromToolSetSpec(spec: Record<string, unknown>, name = ""): ToolSetForm { return { name, description: String(spec.description ?? ""), toolRefs: Array.isArray(spec.toolRefs) ? spec.toolRefs.map((item) => String(asRecord(item).name ?? "")).filter(Boolean) : [], legacy: { ...spec } }; }
export function buildToolSetSpec(form: ToolSetForm): Record<string, unknown> { const spec = { ...form.legacy }; const prior = Array.isArray(form.legacy.toolRefs) ? form.legacy.toolRefs.map(asRecord) : []; delete spec.description; delete spec.toolRefs; if (form.description.trim()) spec.description = form.description.trim(); if (form.toolRefs.length) spec.toolRefs = form.toolRefs.map((name) => ({ ...(prior.find((ref) => String(ref.name ?? "") === name) ?? {}), name })); return spec; }
export function validateToolSetForm(form: ToolSetForm, isCreate: boolean): string | null { if (isCreate && !form.name.trim()) return "Name is required"; if (isCreate && !/^[a-z0-9]([-a-z0-9]*[a-z0-9])?$/.test(form.name.trim())) return "Name must be a DNS-1123 label"; return null; }
