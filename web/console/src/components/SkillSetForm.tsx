import type { SkillSetForm as SkillSetFormModel } from "../pages/library/skillSetForm";
import { OrderedReferenceList } from "./OrderedReferenceList";
interface Props { form: SkillSetFormModel; disabled?: boolean; isCreate: boolean; skillNames: string[]; onChange: <K extends keyof SkillSetFormModel>(key: K, value: SkillSetFormModel[K]) => void; }
export function SkillSetForm({ form, disabled, isCreate, skillNames, onChange }: Props) { const legacy = ["skills", "tools", "subagents"].filter((key) => Array.isArray(form.legacy[key]) && (form.legacy[key] as unknown[]).length); return <div className="guided-form"><section className="explain-panel"><h3 className="explain-title">Ordered skill collection</h3><p className="explain-body">Select independently authored AgentSkills. Personas and embedded definitions are deprecated compatibility inputs.</p></section>{legacy.length ? <div className="banner banner-warn">Legacy fields preserved losslessly: <span className="mono">{legacy.join(", ")}</span>. Use the migration command before removing them.</div> : null}<label className="field"><span className="label">Name</span><input className="input mono" value={form.name} disabled={!isCreate || disabled} onChange={(e) => onChange("name", e.target.value)} /></label><label className="field"><span className="label">Description</span><textarea className="textarea" value={form.description} disabled={disabled} onChange={(e) => onChange("description", e.target.value)} /></label><OrderedReferenceList label="Atomic skills" help="Resolved in this exact order." value={form.skillRefs} suggestions={skillNames} disabled={disabled} onChange={(next) => onChange("skillRefs", next)} /></div>; }
