import type { ToolSetForm as ToolSetFormModel } from "../pages/library/toolSetForm";
import { OrderedReferenceList } from "./OrderedReferenceList";
interface Props { form: ToolSetFormModel; disabled?: boolean; isCreate: boolean; toolNames: string[]; onChange: <K extends keyof ToolSetFormModel>(key: K, value: ToolSetFormModel[K]) => void; }
export function ToolSetForm({ form, disabled, isCreate, toolNames, onChange }: Props) { const legacy = Array.isArray(form.legacy.tools) && form.legacy.tools.length; return <div className="guided-form"><div className="banner banner-warn"><strong>Code-execution authority.</strong> Selecting tools makes their executable acquisition available to consuming runs.</div>{legacy ? <div className="banner banner-warn">Deprecated embedded <span className="mono">tools</span> are preserved losslessly until migrated.</div> : null}<label className="field"><span className="label">Name</span><input className="input mono" value={form.name} disabled={!isCreate || disabled} onChange={(e) => onChange("name", e.target.value)} /></label><label className="field"><span className="label">Description</span><textarea className="textarea" value={form.description} disabled={disabled} onChange={(e) => onChange("description", e.target.value)} /></label><OrderedReferenceList label="Atomic tools" help="Resolved in this exact order." value={form.toolRefs} suggestions={toolNames} disabled={disabled} onChange={(next) => onChange("toolRefs", next)} /></div>; }
